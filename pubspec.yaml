name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  animated_text_kit: ^4.2.2
  awesome: ^0.0.3
  google_fonts: ^6.1.0
  photo_view: ^0.14.0
  url_launcher: ^6.0.0
  font_awesome_flutter: ^10.6.0
  flutter_image_slideshow: ^0.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  assets:
      - assets/megan_sin_headshot.png
      - assets/megan_sin_resume.pdf
  uses-material-design: true
